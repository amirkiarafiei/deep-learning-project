# Copy this file to `.env` and fill in real values.
# `.env` is gitignored — never commit your actual API keys.

# Gemini API key for the v3 label-cleanup pipeline.
# Get one at: https://aistudio.google.com/apikey
GEMINI_API_KEY=
